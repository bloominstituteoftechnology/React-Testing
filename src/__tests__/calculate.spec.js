const calculateFunctions = require('./../logic/calculate.js')

describe('calculations', () => {
    
    it('calculate function should exist', () => {          calculateFunctions.calculate
    });

    // it('calculate function should return buttonName in state', () => {          
        
    //     const state = {next: null}
    //     const btnName = "3";
    //     const actual = calculateFunctions.calculate(state, btnName);
            
    //     expect(actual).toBe({next: "3"});
    // });

});